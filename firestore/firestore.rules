rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All collections: public read, no client writes (only service accounts / Cloud Functions)
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
